[CLI]
# Quality: -ql (low), -qm (medium), -qh (high), -qk (4k)
quality = l
# Preview: open video when done (set to false for batch renders)
preview = true

[output]
# Output directory for rendered media
media_dir = ./media
